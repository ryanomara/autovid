# Desktop App

To run the desktop application:

```bash
cd desktop
npm install
npm start
```

To build distributable packages:

```bash
npm run build
```

This will create platform-specific installers in the `desktop/build` directory.
